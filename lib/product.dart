// Inside some file (e.g., bar_data.dart)
class Product {
  // Public class, now it can be used in other files
  final String name;
  final double value;

  Product({required this.name, required this.value});
}
